# Email provider: secure cPanel SMTP (never commit SMTP_PASS)
SMTP_HOST=qirox.online
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=qodratak@qirox.online
SMTP_PASS=
FROM_EMAIL=Qodratak.Platform@gmail.com
FROM_NAME=مؤسسة قدراتك العالية

# Optional alternative provider. If set, it takes priority over SMTP.
SMTP2GO_API_KEY=

# Server-only AI vision provider. Replit AI Integrations supplies these values
# without exposing credentials to the browser. OPENAI_API_KEY remains supported
# for the existing OpenRouter setup.
AI_INTEGRATIONS_OPENAI_BASE_URL=
AI_INTEGRATIONS_OPENAI_API_KEY=
AI_INTEGRATIONS_OPENAI_MODEL=gpt-4o-mini
